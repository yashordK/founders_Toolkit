# Open Items — Founder's Second Brain

To resolve before Day 5:

- [ ] Exact submission deadline time (not just date) — check Discord/registration form.
- [x] ~~Cognee's LLM (xAI/Grok) had no credits, OpenAI embedding key was over quota~~ — resolved 2026-07-05: LLM switched to `openrouter/nvidia/nemotron-3-ultra-550b-a55b:free` (OpenRouter, LLM_ENDPOINT="https://openrouter.ai/api/v1"), embeddings switched to `fastembed` (local, free, no API key — added the `fastembed` extra to `cognee/Dockerfile` and rebuilt). See below for a new, more serious issue found using this LLM.
- [ ] **BLOCKING, HIGH PRIORITY:** `openrouter/nvidia/nemotron-3-ultra-550b-a55b:free` **confirmed hallucinating facts with no basis in the graph**, more than once, unprompted. Reproduced 2026-07-05: seeded a dataset with exactly one contact (Diego Ramirez) and one task; `recall()` (via both the `warm-intro` and `daily-plan` skills) returned additional fabricated meetings/tasks/priorities that sounded specific and plausible. Proved they were fabricated (not a Cognee data-persistence bug) by checking `GET /api/v1/datasets/{id}/graph` directly — the real graph had none of it. This is a serious risk for a product whose entire pitch is trustworthy memory, not a minor caveat — every skill built so far (add-contact, add-meeting, quick-note, warm-intro, daily-plan, nudge-sweep) shares this same recall()/remember() pipeline and model. Needs a decision before the live demo: try a different (possibly paid) model, add a verification step that cross-checks recall() answers against `/graph` or `/data` before they reach the user, or explicitly scope the demo script to avoid open-ended recall questions where a fabrication would be visible.
- [ ] Decide final demo script: which contact gets added live, which meeting transcript is pre-seeded vs added live.
- [x] ~~Confirm Whisper (or alternative STT) integration path for offline meeting voice memos~~ — resolved: OpenClaw ships a bundled `openai-whisper` skill (local Whisper CLI, no API key). Not installed yet on this dev machine (shows "needs setup", requires the `whisper` binary — the bundled skill's install helper is brew/macOS-specific, so Windows needs a separate `pip install openai-whisper` or similar). `add-meeting`'s SKILL.md already accounts for this and falls back to asking for pasted text if the binary isn't present.
