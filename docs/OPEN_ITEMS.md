# Open Items — Founder's Second Brain

To resolve before Day 5:

- [ ] Exact submission deadline time (not just date) — check Discord/registration form.
- [x] ~~Cognee's LLM (xAI/Grok) had no credits, OpenAI embedding key was over quota~~ — resolved 2026-07-05, in two more steps than expected. First switched to `openrouter/nvidia/nemotron-3-ultra-550b-a55b:free` (hit OpenRouter's 50-req/day free cap almost immediately, doubled by the verification step added below). Then switched to a new xAI key + `grok-4.1-fast` (that model name doesn't exist on xAI's API, and the new key turned out to be the same uncredited xAI team as before — same team ID in the error). Landed on: **LLM = local LM Studio** (`openai/deepseek-r1-distill-llama-8b-abliterated` at `LLM_ENDPOINT=http://192.168.29.160:1234/v1`, `LLM_INSTRUCTOR_MODE=json_schema_mode` per cognee's own LM Studio troubleshooting note), **embeddings = `fastembed`** (local, free, no API key — added the `fastembed` extra to `cognee/Dockerfile` and rebuilt). No rate limit, no billing, ~4-20s per call depending on task. See below for what was actually found using this setup.
- [x] ~~Hallucination: recall() confirmed fabricating facts with no basis in the graph, more than once, unprompted~~ — **mostly root-caused and fixed 2026-07-05, smaller residual risk remains with a working mitigation.** What first looked like confident, elaborate hallucination (a whole fabricated meeting, tasks, a standing priority — proven fake via `GET /api/v1/datasets/{id}/graph`) turned out to be **mostly cognee's own session-turn logic** (`cognee/infrastructure/session/session_turn.py`): every test call omitted `session_id`, so cognee treated repeated/similar test queries as turns in one ongoing default session, sometimes rewriting the effective query off stale context, sometimes short-circuiting entirely with a bare `"Got it."` (that exact string is a hardcoded fallback in `graph_completion_retriever.py`, not a model quirk). Fixed in all three recall()-based skills (`warm-intro`, `daily-plan`, `nudge-sweep`) by passing `sessionId: crypto.randomUUID()` per call. **After that fix, a smaller residual risk remains**: the model can still add one minor ungrounded embellishment on top of an otherwise-correct answer (e.g. inventing "current projects" to leverage a contact's expertise for, when nothing about projects exists in the graph) — reproduced even with session isolation in place. Mitigated by `agent/verify/verify_recall.mjs`, added the same day: fetches the real graph and asks a second LLM call to flag any unsupported claim before a skill relays or sends an answer. Confirmed working in back-to-back runs (let a correct answer through, blocked one with a genuine fabricated addition). Net: this is no longer a "don't trust anything" situation, but the mitigation is a same-day addition with limited mileage — spot-check before the live demo rather than assuming it's bulletproof.
- [ ] Decide final demo script: which contact gets added live, which meeting transcript is pre-seeded vs added live.
- [x] ~~Confirm Whisper (or alternative STT) integration path for offline meeting voice memos~~ — resolved: OpenClaw ships a bundled `openai-whisper` skill (local Whisper CLI, no API key). Not installed yet on this dev machine (shows "needs setup", requires the `whisper` binary — the bundled skill's install helper is brew/macOS-specific, so Windows needs a separate `pip install openai-whisper` or similar). `add-meeting`'s SKILL.md already accounts for this and falls back to asking for pasted text if the binary isn't present.
